class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int a=INT_MIN+100000;
        int ans=0;
        for(int i=1;i<prices.size();i++){
            int dif=prices[i]-prices[i-1];
            a=max(a+dif,dif);
            ans=max(ans,a);
        }
        return ans;
    }
};